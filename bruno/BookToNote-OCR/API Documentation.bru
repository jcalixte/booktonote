meta {
  name: API Documentation
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/
  body: none
  auth: none
}

docs {
  Get API documentation and available endpoints.

  Expected response:
  {
    "service": "BookToNote OCR API",
    "version": "1.0.0",
    "endpoints": {
      "POST /ocr": "Upload an image (jpg, png, tiff, pdf) for text extraction",
      "GET /health": "Check service health status"
    }
  }
}
